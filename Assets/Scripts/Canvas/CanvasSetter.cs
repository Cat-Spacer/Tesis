using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanvasSetter : MonoBehaviour
{
    Canvas _canvas;
    void Start()
    {
        //_canvas.renderMode{2};
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
